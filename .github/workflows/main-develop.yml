name: My Sample (develop)

on:
  push:
    branches:
      - develop

jobs:
  deploy_develop:
    runs-on: ubuntu-latest
    environment:
      name: develop
    steps:
      - uses: actions/github-script@v3
        id: write-json
        with:
          script: |
            return {
              "project_id": "${{ vars.PROJECT_ID }}"
            }
          result-encoding: json
      - run: echo '${{steps.write-json.outputs.result}}' >> hoge.json
      - run: cat hoge.json
